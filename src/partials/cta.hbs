<section class="cta">
  <div class="container-fluid text-center">
    <div class="row">
      <div class="col-md-12">
        <a class="btn-new" href="{{link1}}">{{btn1-text}}</a> 
        <a class="btn-new" href="{{link2}}" style="margin-left: 50px; color: $secondary-color-lightest;">{{btn2-text}}</a>
      </div>
    </div>
  </div>
</section>
